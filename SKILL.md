---
name: gold-foil-card-studio
description: 将描述或参考图制作成全金卡或局部烫金闪卡，用全卡对齐线稿、反射坐标、颗粒扫光与辉光生成转卡特效，交付可编辑 Blender 材质及多角度渲染，可按需迁移网页。适用于金卡、烫金卡、gold foil card；不是实体印刷、彩虹全息或视差卡。
---

# Gold Foil Card Studio

把用户的主题变成可编辑、随观察角度变化的烫金闪卡。采用 Holo Card Studio 的“素材契约 → 确定性构建 → 实际渲染验收”思路，不把彩虹材质简单染黄。

## 制作

1. 确定输出目录、主体、画风、文字和交付目标。先读 [references/method.md](references/method.md)。用户要“全金卡”时，整张设计应是香槟金/古金色系，主体、背景纹样、边框和文字都参与烫金，不能交付彩色卡面加主体金边。只有要求局部烫金时才保留相应局部范围。默认竖版 2:3；猫咪和新艺术风只是视频示例，不是通用硬性风格。
2. 准备同尺寸、完全对齐的卡面和白底黑线线稿。线稿应覆盖目标设计的完整范围，不等于抠图轮廓，不可用 `structure × character × (1-ui)` 代替全卡线稿。此流程不要求四层视差。已有分层作品可以先合成卡面，但保留原始图层。
   - 全金版可用一张干净黑白线稿作为共同母版：`artwork` 与 `lineart` 指向同一 PNG，设置 `printPalette.ink` / `stock`（线性 RGB）分别映射古金墨线和香槟金底。这样印刷线与发光线天然同源配准；不要把静态金属反光照片当母版。彩色版仍用原画与对齐线稿两张 PNG，不设置 `printPalette`。
   - 有参考图时先查看，明确它是身份参考、风格参考还是直接编辑目标。保留指定主体、姿势、构图和准确文字。
   - 从确认的彩色图编辑出线稿，提示词示例：“保持像素级构图和人物位置，只提取单一黑色轮廓和重要内部细线；白底，不要阴影、排线或黑色填充，不新增装饰。”不要仅凭同一段文字重新生成一张不对齐的图。
   - 生成新素材时使用当前可用的图像生成能力；没有时说明缺少素材，请用户提供，不冒称已经生成。文字优先用真实字体精确排版，并同步到两张图；别把不正确的 AI 文字固化到卡面。
   - 目视检查眼睛、衣褶、背景、边框与文字的配准。全卡线稿各区域都要有有效细节，不能以平均覆盖率代替目视；灰色背景填充会使整片底色误发光，须修正。自动边缘提取只能作为明确标注的预览草稿，不能冒充生产线稿。
3. 参照 [references/config.example.json](references/config.example.json) 写 `<project>/card-config.json`。所有参数值都是此实现的可调起点，不宣称逐项复刻原视频的数值。用户给定的图不必重新生成。
4. 查找已安装的 Blender，优先使用 4.5 LTS；缺少时说明并通过正常授权安装官方版本，不自动安装图像平台、插件或付费服务。用新的 `--output` 目录运行以下命令（替换为实际绝对路径）：

   ```sh
   /path/to/blender --background --factory-startup --python-exit-code 1 \
     --python /path/to/gold-foil-card-studio/scripts/build_card.py -- \
     --project /path/to/MyCard --output /path/to/MyCard/output-v1
   ```

   不要省略 `--python-exit-code 1`，否则 Blender 脚本异常可能仍返回成功退出码。脚本拒绝覆盖非空目录，素材打包进 `.blend`，不改全局 Blender 偏好。`--skip-render` 只用于调试，不代表验收完成。
5. 检查 `gold-card.blend` 的 `Gold Foil / Reflection Sweep` 节点组与三个角度渲染。对照来源视频约 02:25 的完整线稿、02:34 的金线分布、02:54 的转卡辉光，分别核对金色覆盖、明暗区域迁移、细颗粒与辉光范围。全金版还需确认关闭发光后仍是金色设计，背景、边框、文字和主体各区域在角度扫描中都出现亮暗变化。输出像素非零或自动测试 PASS 不能证明复刻成功。过曝时先降低发光或收窄光带，不能靠辉光掩盖配准错误。记录素材、提示词、参数与未能核实的差异。
6. 交付工程与渲染的真实路径。用户需要网页时再读 [references/web-port.md](references/web-port.md)，在现有查看器内重建同一材质并实际浏览器验证。不要直接交付原 Holo Card Studio 的彩虹 shader 冒充金卡；普通 GLB 和现有 BlinkCard 导入器不会自动执行 Blender 程序化节点。

## 检查与边界

- 核心：Reflection → Mapping，加上细噪波扰动 → Multiply → Ping-Pong → 黑白渐变，控制印刷材质与金色线稿材质的混合。线稿遮罩和角度遮罩各司其职。
- 发光源只在选定线条和当前扫光覆盖处；辉光可扩散至周围或卡片之外，但白底不能成为发光源。不要默认加入彩虹、粒子、视差或动画时间驱动。
- 可调旋钮：扫光方向/相位、重复次数、光带宽度、颗粒尺度/强度、线稿阈值、金色及亮度。保持可编辑，不把动态反光烘焙成一张静态“金卡照片”。
- 改动构建脚本后运行 `scripts/smoke_test.py --blender /path/to/blender`（测试额外需要 Pillow）。测试素材只验证材质通路，不是用户成品；测试失败时保留报告、定位失败，不声明完成。
- 打包 skill 只包含本目录的文本源码，不包含用户原画、视频、`.blend`、渲染、依赖或缓存。上游教程与代码来源见 `references/method.md`；其公开可读不等于获得猫咪插画的商业分发权。
